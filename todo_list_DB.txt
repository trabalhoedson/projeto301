CREATE TABLE tasks(
       id INT AUTO_INCREMENT PRIMARY KEY, 
	tittle VAR CHAR(100) NOT NULL,
	status ENUM ('pendente','concluida')DEFAULT 'pendente',
	creted_at DATETIME DEFAULT CURRENT_TIMESTAMP);

	
	